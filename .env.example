# ==================================
# Blender Docs MCP Server 環境變數設定
# ==================================
# 請複製此檔案為 .env 並填入您的 API Keys

# OpenAI API Key
# 用於生成文件的向量嵌入 (embeddings)
# 取得方式：
# 1. 前往 https://platform.openai.com/api-keys
# 2. 登入您的 OpenAI 帳號
# 3. 點擊 "Create new secret key"
# 4. 複製生成的 key (格式: sk-...)
# 注意：免費帳號有使用限制，但通常足夠個人使用
OPENAI_API_KEY=sk-...

# Pinecone API Key
# 用於儲存和搜尋向量資料庫
# 取得方式：
# 1. 前往 https://app.pinecone.io/
# 2. 註冊或登入帳號（有免費方案）
# 3. 在 Dashboard 中找到 "API Keys" 區域
# 4. 複製 Default API key
# 格式通常為: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
PINECONE_API_KEY=...

# Pinecone Index Name
# 向量資料庫的索引名稱
# 如果索引不存在，需要先在 Pinecone 控制台創建：
# 1. 在 Pinecone Dashboard 點擊 "Create Index"
# 2. 設定：
#    - Name: blender-docs (或您想要的名稱)
#    - Dimensions: 1536 (對應 text-embedding-3-small)
#    - Metric: cosine
#    - Cloud: 選擇離您最近的區域
# 3. 等待索引創建完成（約 1 分鐘）
PINECONE_INDEX_NAME=blender-docs

# ==================================
# 選擇性設定
# ==================================

# 快取設定（選擇性）
# 設定快取目錄位置，預設為 .cache
# BLENDER_CACHE_DIR=.cache

# 快取 TTL（選擇性）
# 快取有效時間（秒），預設為 86400（24小時）
# CACHE_TTL_SECONDS=86400

# ==================================
# 成本預估
# ==================================
# OpenAI:
# - text-embedding-3-small: $0.02 / 1M tokens
# - 初始索引 2000+ 文件：約 $0.10
# - 每次查詢：< $0.001
#
# Pinecone:
# - 免費方案：100K vectors, 1個索引
# - 對個人使用來說綽綽有餘
# ==================================